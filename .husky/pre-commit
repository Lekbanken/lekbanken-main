#!/usr/bin/env sh
# Husky pre-commit — Nivå A (fast, staged files only)
# Runs ESLint on staged TS/TSX + action-validator on staged YAML via lint-staged.
# Also runs quick typecheck on the full project.
# Bypass: git commit --no-verify

npx lint-staged --allow-empty

npm run type-check
