/**
 * Vitest Setup File
 * 
 * Centralized test setup for all unit and component tests.
 * Imported automatically via vitest.config.ts setupFiles.
 */

// Add jest-dom matchers for DOM assertions
import '@testing-library/jest-dom/vitest';
